# ==========================================
# Local LLM Research Analytics Tool
# Environment Configuration Template
# ==========================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control
# ==========================================

# ------------------------------------------
# Ollama Configuration
# ------------------------------------------
# Host URL for Ollama server
OLLAMA_HOST=http://localhost:11434

# Model to use for inference (must support tool calling)
# 
# RTX 5090 (32GB VRAM) - RECOMMENDED:
#   qwen2.5:32b-instruct     - Best quality, excellent tool calling
#   llama3.3:70b-instruct-q4_K_M - Near GPT-4 quality (uses ~38GB, partial CPU offload)
#   deepseek-r1:32b          - Superior reasoning, slower
#   qwen2.5-coder:32b        - Best for code-heavy queries
#
# RTX 4090/3090 (24GB VRAM):
#   qwen2.5:14b-instruct     - Great balance
#   llama3.1:8b-instruct     - Fast, reliable
#
# RTX 4080/3080 (16GB VRAM):
#   qwen2.5:7b-instruct      - Best tool calling at this size
#   mistral:7b-instruct      - Fast alternative
#
# CPU-Only (requires lots of RAM):
#   qwen2.5:7b-instruct-q4_K_M - Quantized for speed
#
OLLAMA_MODEL=qwen2.5:32b-instruct

# ------------------------------------------
# SQL Server Configuration (Docker)
# ------------------------------------------
# For Docker SQL Server (docker/docker-compose.yml)
# Run: cd docker && docker compose up -d && docker compose --profile init up

# SQL Server hostname or IP address
SQL_SERVER_HOST=localhost

# SQL Server port (default: 1433)
SQL_SERVER_PORT=1433

# Database name (use ResearchAnalytics for the sample database)
SQL_DATABASE_NAME=ResearchAnalytics

# Trust the server certificate (required for Docker/self-signed certs)
SQL_TRUST_SERVER_CERTIFICATE=true

# ------------------------------------------
# SQL Server Authentication
# ------------------------------------------
# Docker SQL Server uses SQL Authentication (sa account)
# For Windows Auth on local SQL Server, leave USERNAME/PASSWORD blank

# SQL Server username (sa for Docker)
SQL_USERNAME=sa

# SQL Server password (match MSSQL_SA_PASSWORD in docker-compose.yml)
SQL_PASSWORD=LocalLLM@2024!

# ------------------------------------------
# Docker SQL Server Configuration
# ------------------------------------------
# Password for sa account in Docker SQL Server
# Must meet SQL Server password policy: 8+ chars, upper+lower+number+symbol
MSSQL_SA_PASSWORD=LocalLLM@2024!

# ------------------------------------------
# MSSQL MCP Server Configuration
# ------------------------------------------
# Path to the MSSQL MCP Server index.js file
# After cloning and building: SQL-AI-samples/MssqlMcp/Node/dist/index.js
MCP_MSSQL_PATH=/path/to/SQL-AI-samples/MssqlMcp/Node/dist/index.js

# Set to "true" for read-only mode (safer for exploration)
MCP_MSSQL_READONLY=false

# ------------------------------------------
# Application Configuration
# ------------------------------------------
# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Streamlit server port
STREAMLIT_PORT=8501

# ------------------------------------------
# Development Settings
# ------------------------------------------
# Enable debug mode (more verbose output)
DEBUG=false

# Enable MCP server debug logging
MCP_DEBUG=false
